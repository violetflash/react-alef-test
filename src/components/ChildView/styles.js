import styled from 'styled-components/macro';

export const StyledChild = styled.span`
  padding: 10px 20px;
  color: var(--black);
  background-color: var(--input-border-color);
  font-size: var(--fz-accent);
  font-weight: var(--fw-bold);
  border-radius: 5px;
`;

