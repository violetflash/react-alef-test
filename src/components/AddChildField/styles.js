import styled from 'styled-components/macro';

export const AddChildContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  
  label {
    margin: 0;
    max-width: calc((100% - 96px) / 2);
  }
`;

